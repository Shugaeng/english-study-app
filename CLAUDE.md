# english-study-app

단일 파일(`index.html`)로 구성된 영어 단어/독해 학습 웹앱입니다. 별도 빌드 과정 없이
브라우저에서 바로 열어 사용하며, 데이터는 `localStorage`(+선택적으로 Firebase Firestore)에 저장됩니다.

## 버전 관리 규칙 (중요)

`index.html`의 JS 상단에 있는 `APP_VERSION` 상수는 현황(대시보드) 탭 우측 하단에
`shugaeng · v버전` 형태로 표시됩니다.

**앱에 기능 추가/변경/버그 수정 등 사용자에게 보이는 업데이트를 커밋할 때마다,
`APP_VERSION` 값을 반드시 한 단계 올려주세요.**

- 버전 형식: `vMAJOR.MINOR.PATCH` (예: `v1.0.0` → `v1.0.1`)
- 자잘한 수정/버그 픽스: PATCH 증가 (예: v1.0.0 → v1.0.1)
- 기능 추가: MINOR 증가 (예: v1.0.1 → v1.1.0)
- 큰 구조 변경: MAJOR 증가
- 애매하면 PATCH를 올리는 쪽으로 판단하면 됩니다.
- 문서/주석만 고치는 등 사용자에게 보이지 않는 변경은 버전을 올리지 않아도 됩니다.

## 말하기 탭 Day별 JSON 만들어줄 때 (중요)

사용자가 교재 페이지 사진을 보내면, 해당 Day의 에피소드들을 아래 형식의 JSON 배열로
만들어 `SendUserFile`로 전달합니다 (앱의 "설정 → 말하기 문장 관리 → 가져오기"로 바로
가져올 수 있는 형식):

```json
[
  {
    "title": "[1] 에피소드 제목",
    "ko": "한글 전체 문단...",
    "en": "English full paragraph...",
    "vocab": ["표현1:뜻1", "표현2:뜻2"]
  },
  ...
]
```

- **`title` 필드를 반드시 포함할 것** — 교재에 적힌 실제 에피소드 제목([1] 후회 제로
  러닝 같은 형식)을 그대로 옮겨 적습니다. title이 없으면 가져오기 시 "[n] 에피소드 n"
  같은 임시 제목이 붙어서 사용자가 일일이 이름변경을 해야 합니다.
- `ko`/`en`은 문장 단위로 미리 쪼개지 말고, 마침표까지 포함한 문단 전체를 그대로 넣습니다
  (앱이 가져오기 시 마침표 기준으로 자동으로 문장을 나눕니다).
- **`vocab` 필드에 예문 아래 각주(핵심 표현/숙어)도 반드시 함께 옮겨 적을 것** — 사진에서
  "└ usual cafe 단골 카페 | focus 집중하다" 같은 각주 줄이 보이면, `"표현:뜻"` 형식의
  문자열 배열로 만들어 넣습니다 (예: `["usual cafe:단골 카페", "focus:집중하다"]`).
  이 값은 말하기 결과 화면(정답 확인 시)에 "📌 핵심 표현"으로 함께 표시됩니다.
  각주가 없는 에피소드는 `vocab`을 빈 배열 `[]`로 두거나 필드 자체를 생략해도 됩니다.
- **같은 에피소드가 교재에서 두 페이지(INPUT 페이지 + STEP3/4 연습 페이지)에 걸쳐
  나오고, 각 페이지마다 서로 다른 각주가 달려있을 수 있습니다.** 한쪽 페이지만 보고
  다른 페이지의 각주를 빠뜨리지 않도록, 같은 Day의 에피소드 사진을 여러 장 받으면
  페이지별 각주를 전부 `vocab` 배열에 합쳐서 넣습니다 (하나를 다른 하나로 덮어쓰지
  말 것 — 문장/제목이 같은 에피소드면 각주는 추가하는 것입니다).

## 가사 탭 곡 JSON 만들어줄 때 (중요)

사용자가 영어 노래 가사를 보내면(텍스트로 붙여넣거나 사진으로), 아래 형식의 JSON
배열로 만들어 `SendUserFile`로 전달합니다 (앱의 "설정 → 가사 관리 → 가져오기"로 바로
가져올 수 있는 형식):

```json
[
  {
    "title": "곡 제목 - 아티스트",
    "en": "영어 가사 1번째 줄\n영어 가사 2번째 줄\n...",
    "ko": "한글 번역 1번째 줄\n한글 번역 2번째 줄\n..."
  }
]
```

- **`en`/`ko`는 줄바꿈(`\n`)으로 구분하고, 반드시 같은 줄 수로 한 줄씩 정확히
  대응시킵니다.** 말하기 탭과 달리 마침표 기준이 아니라 실제 가사의 줄바꿈 그대로
  나누므로, 원곡 가사의 줄 구성을 그대로 따라가면 됩니다.
- `ko`는 **직역이 아니라 가사의 의미가 자연스럽게 와닿는 번역**으로 만듭니다 —
  연습 화면에서 이 줄을 보고 사용자가 해당 영어 가사를 기억해내는 용도라, 지나치게
  기계적인 번역보다는 뜻이 통하는 번역이 더 유용합니다.
- 후렴구(코러스)처럼 가사에 반복되는 줄은 등장할 때마다(반복 횟수만큼) `en`/`ko`에
  그대로 다시 적어줍니다 — 나중에 배열을 합칠 때 중복을 임의로 생략하지 않습니다.
- 제목은 "곡 제목 - 아티스트" 형식을 기본으로 하되, 사용자가 원하는 표기가 있으면
  그걸 따릅니다.
